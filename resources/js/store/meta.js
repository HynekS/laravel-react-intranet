import client from "../utils/axiosWithDefaults"

const initialState = {
  
}

// Actions


// Reducer
export default function reducer(state = initialState, action = {}) {
  switch (action.type) {
    
    default:
      return state
  }
}

// Action creators

// Thunks

